import SwiftUI

struct JobListScreen: View {
    var body: some View {
        NavigationStack {
            EmptyView(message: "공고 목록 (구현 예정)")
                .navigationTitle("공고")
        }
    }
}
