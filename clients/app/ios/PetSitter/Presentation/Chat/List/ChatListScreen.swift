import SwiftUI

struct ChatListScreen: View {
    var body: some View {
        NavigationStack {
            EmptyView(message: "채팅 목록 (구현 예정)")
                .navigationTitle("채팅")
        }
    }
}
